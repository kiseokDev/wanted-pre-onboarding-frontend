import { render } from "@testing-library/react";
import { TodoList } from "../../components";
import TodoInsert from "../../components/todo/TodoInsert";

describe("Todo 컴포넌트 테스트", () => {
    it("/todo경로에 접속하면 투두 리스트의 아이템이 화면에 보여진다", () => {
    });
});